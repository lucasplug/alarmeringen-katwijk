# Lokaal ontwikkelen: bouwt het image uit deze repo en leest de configuratie
# uit een .env-bestand.
#
#   docker compose -f docker-compose.dev.yml up -d --build
services:
  alarmeringen-mqtt-bridge:
    build: .
    image: alarmeringen-mqtt-bridge:dev
    container_name: alarmeringen-mqtt-bridge-dev
    restart: unless-stopped
    env_file:
      - .env
    environment:
      LOCATION_NAME: "${LOCATION_NAME:-Katwijk}"
      COUNTRY_NAME: "${COUNTRY_NAME:-Nederland}"
      TZ: Europe/Amsterdam
    volumes:
      - alarmeringen-data:/app/data

volumes:
  alarmeringen-data:
